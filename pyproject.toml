[project]
name = "frogbed-dirty-darts"
version = "0.1.0"
description = "A darts roguelike prototype built milestone-by-milestone."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]